# Platform

This folder contains the code required to build the infrastructure platform for the Demo-Factory.

The structure is:
```
platform/
├── docker
└── scripts
```

The `docker` folder contains all Docker-related configurations for the various components, and the `scripts` folder contains the scripts used to manage the platform.
