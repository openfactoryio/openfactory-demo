# openfactory-demo
Single node OpenFactory demo

## Quick start

1. Define `HOST_IP` environment variable with the IP adress of the host

2. Deploy the platform with
   ```bash
   ./platform/scripts/deploy_platform.sh
   ```
